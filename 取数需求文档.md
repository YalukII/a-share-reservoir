# 需求文档：A股「蓄水池」监控仪表盘 — 取数与自动更新

> 给实现方(Claude Code / 开发)：本文档描述要做的取数脚本与定时部署。**前端 HTML 已完成、不要改**；你的产出是 `pull.py`（取数）、GitHub Actions 工作流、部署说明。一切数据都写进 `data.json`，HTML 负责渲染。

---

## 0. 背景与目标

- 已有两文件：`A股蓄水池监控仪表盘.html`（只读 `data.json` 渲染，**不改**）、`data.json`（数据，当前多为示例占位）。
- **目标**：写脚本定时拉取**真实**宏观/资金数据，更新 `data.json`，并部署成网页定时自动刷新。
- **核心原则**：数据与展示分离；脚本只负责按既定 schema 生产 `data.json`。

## 1. 技术栈与约束

- **Python 3.10+**。取数首选 **akshare**（免费、无需 token）；备选 **Tushare**（需 token，放 GitHub Secrets，不能进代码/data.json）。
- ⚠️ **akshare 的接口名与返回列名随版本变化**——实现时务必对照官方文档（akshare.akfamily.xyz）校对函数名和列名，不要照抄本文档里的函数名。
- ⚠️ **东方财富等网页是 JS 动态渲染**，不要直接爬 HTML（爬不到表格数字）；用 akshare/官方接口。
- ⚠️ HTML 通过 `fetch('data.json')` 读数据，**必须经 HTTP 服务访问**（GitHub Pages 即可）；`file://` 直接打开会回退到内置示例。

## 2. `data.json` 数据契约（脚本必须产出/维护此结构）

顶层字段：`meta` / `snapshot` / `groups` / `checklist` / `tools` / `sources` / `assessment` / `tierLegend`。
**脚本只需更新**：`snapshot[].value/series/note/period`、`checklist[].now`、`assessment`、`meta.updated/snapshotLabel/dataNote`。其余（groups 文案、tools、sources）保持人工维护，脚本不要动。

### 2.1 `snapshot[]`（顶部读数卡，每项一条折线）
```jsonc
{
  "id": "m2",                 // 稳定主键，勿改：m2 / m1 / scissor / tsf / team
  "label": "M2 同比",
  "unit": "%",                // 展示单位
  "axis": "同比 %",            // 折线纵轴标注
  "value": 8.6,               // 最新值（脚本更新）
  "tier": "同步",              // 领先/同步/滞后（人工设定，勿动）
  "period": "近12月 · 同比(YoY)",
  "note": "4月末余额约353万亿", // 脚本可更新（日期/口径）
  "desc": "……",               // 说明文案，人工，勿动
  "series": [ ...12个数 ]      // 近12个月，时间升序（最老→最新），脚本更新
}
```
要求：`series` 固定 **12 个点、按月、时间升序**；`value` = `series` 最后一个；数值四舍五入到 1–2 位。

### 2.2 `checklist[]`（退潮信号，水位计据此自动判定）
```jsonc
{ "ct": "...", "cd": "...", "idx": "...", "now": false }
```
脚本据 §5 规则计算 `now`（true=该退潮信号当前触发）。`ct/cd/idx` 文案勿动。

### 2.3 `assessment`（自动判定结论，显示在水位计顶部）
```jsonc
{ "asOf": "...", "summary": "...", "rationale": ["...", ...], "watch": "..." }
```
由 §5 规则生成（summary 由触发数映射，rationale 由各读数模板化生成）。

### 2.4 `meta`
```jsonc
{ "snapshotLabel": "SNAPSHOT · 2026年4月", "updated": "2026-04", "dataNote": "..." }
```
`updated` 为 `YYYY-MM`，前端用它推算折线横轴的 12 个月标签——**必须等于 series 最新点所在月份**，否则横轴与数据错位。

## 3. 取数清单（逐项；函数名仅供参考，须校对）

| id / 字段 | 含义与口径 | 数据源 / akshare 参考 | 频率 | 变换 | 单位 |
|---|---|---|---|---|---|
| `m2` | M2 同比增速 | `macro_china_money_supply`（取"M2 同比"列） | 月 | 取近12月同比 | % |
| `m1` | M1 同比增速（注：2025 起新口径） | 同上（"M1 同比"列） | 月 | 近12月同比 | % |
| `scissor` | M2–M1 剪刀差 | **计算** = m2.series − m1.series（逐月相减） | 月 | 派生 | 百分点 |
| `tsf` | **社融存量(余额)** | 央行月度金融数据；akshare 社融多为"增量"，存量需另找接口或由增量累计/官方值 | 月 | 取余额(万亿)，近12月；note 写同比% | 万亿 |
| `team` | 国家队 ETF 持仓(季报) | 基金季报"前十大持有人"汇总（滞后） | 季 | 余额(万亿) | 万亿 |
| 两融(C组) | 融资融券余额 沪+深 | `stock_margin_sse` + `stock_margin_szse` 合并 | 日 | 最新+近趋势 | 亿元 |
| CPI/PPI(F组) | CPI、PPI 同比 | `macro_china_cpi_yearly` / `macro_china_ppi_yearly` | 月 | 同比 | % |
| 汇率(F组) | 人民币兑美元(CFETS/在岸) | 外汇接口 | 日 | 最新 | — |
| ETF份额(B组) | 宽基ETF日度份额净申赎 | ETF 基金"份额"接口（沪深300/上证50/科创50/中证1000 等龙头ETF）；**注意是"份额"不是"净值"** | 日 | 近N日份额净变化 | 亿份 |

> 拿不到的源：失败时**保留旧值并打 warning**，不要让整脚本崩。

## 4. 六组核心指标的处理（重要区分）

- **数值型，可自动更新**：A(货币)、C(两融/估值)、D(IPO募资额)、E(存款搬家=居民/非银存款增量)、F(CPI/PPI/汇率)。可给这些组的 row 增加可选字段 `"current": "蓄水"|"中性"|"退潮"`，由 §5 规则算出。
- **质性/事件型，无法用单一数字拉取，保持人工或规则近似**：
  - B 的"平准表态"、G"政策口风" → 事件/措辞类，**不强求自动**（可选：对央行/证监会公告做关键词匹配，识别"维护稳定/活跃市场" vs "防风险/防过热"，作为弱信号）。
  - B 的"ETF 份额"是可自动的（见上）；"汇金季报持仓"是季度滞后的。
- 未做自动判断的项，`current` 留空即可，前端不显示。

## 5. 自动判定规则（脚本计算 `checklist[].now` 与 `assessment`）

按下列规则把数值映射成"退潮触发"。阈值是建议初值，可调：

| 退潮信号(对应 checklist 顺序) | 触发规则(now=true 条件) | 自动化程度 |
|---|---|---|
| 1 政策口风转"防过热" | 公告关键词命中"防风险/抑制过热/防止脱实向虚" | 选做(NLP)，否则人工 |
| 2 两融/杠杆过热 | 两融余额近20交易日涨幅 > 15% **或** 占流通市值比 > 历史90分位 | 可自动 |
| 3 宽基ETF持续净赎回 | 龙头宽基ETF合计份额近10交易日净变化 < 0 | 可自动 |
| 4 货币转紧 | 政策利率上调 **或** M2同比连续2个月下行 | 可自动 |
| 5 IPO提速 | 当月IPO募资额环比 > 50%（或较近6月均值显著放大） | 可自动 |
| 6 汇金季报减持 | 最新季报持仓较上季下降 | 可自动(季度) |
| 7 通缩结束 | CPI同比 > 0 **且** PPI同比 > 0，连续3个月 | 可自动 |

**`assessment.summary` 映射**（触发数 = `now==true` 的条数）：
- 0–2 → `"仍在蓄水 · 水位偏高"`
- 3–4 → `"攻守转换中"`
- 5–7 → `"退潮信号密集"`

`rationale` 用模板按当前读数生成（如 `f"M2 同比 {m2}% …"`）；`watch` 列出尚未触发但最该盯的领先信号。

## 6. 脚本行为要求 (`pull.py`)

1. 读现有 `data.json`（保留人工字段）→ 仅更新 §2 指定字段 → 写回（`ensure_ascii=False, indent=2`）。
2. **鲁棒**：每个指标独立 try/except，失败保留旧值 + 打印 warning。
3. **幂等**：可重复运行，结果只取决于当前数据源。
4. 运行后打印一行摘要（更新了哪些、哪些失败）。

## 7. 定时与部署

- **GitHub Actions**：`cron` 定时（建议每月金融数据发布后，如每月 15 日；或每周一）运行 `pull.py` → `git add data.json` → commit & push。
- **GitHub Pages**：发布该仓库；网址公开（任何人凭链接可访问，本仪表盘无敏感信息，但 **token 必须放 Actions Secrets，不能进仓库**）。
- workflow 要点：`actions/checkout` → `setup-python` → `pip install akshare pandas` → `python pull.py` → `git commit/push`（用 `GITHUB_TOKEN` 或 PAT）。

## 8. 验收标准

- [ ] 经 HTTP 打开网页，5 条读数为**真实最新值**，折线为近12个月、横轴月份与最新数据月对齐。
- [ ] 水位计"当前判定"由数据自动算出，且与 `checklist[].now` 一致。
- [ ] 至少自动化 §5 中第 2/3/4/5/7 条退潮信号。
- [ ] 任一数据源失败时，其余指标仍正常更新（容错）。
- [ ] GitHub Actions 能定时跑通并自动提交 `data.json`，网页随之刷新。

## 9. 注意事项 / 已知坑

- akshare 函数名、返回列名按版本校对，别照抄本文档。
- **社融要"存量(余额)"不是"增量"**；央行接口/口径确认。
- M1 自 2025 年起为新口径（含个人活期等），新旧不可直接比。
- **ETF 抓"份额"不是"净值"**；只盯几只龙头宽基。
- `meta.updated` 必须等于 series 最新点月份，否则前端横轴错位。
- `file://` 直接打开读不到 `data.json`，需 HTTP 服务。
- 页面已有免责声明：仅信息梳理，非投资建议。

---

### 附：现有文件
- `A股蓄水池监控仪表盘.html`：前端，只读 `data.json`，**勿改**。内含一份内置兜底数据（`<script type="application/json" id="fallback-data">`），可在脚本里顺便同步，或忽略（HTTP 部署后以外部 `data.json` 为准）。
- `data.json`：当前已有完整结构与字段示例，**以它为 schema 标准**。社融(`tsf`)已是 2026 年 4 月央行真实存量值，可作为"真实数据长什么样"的范例。
